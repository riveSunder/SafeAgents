from setuptools import setup

setup(name="safe_agents",
        version="0.0.0.0.0",
        packages=["safe_agents"],
        install_requires=["gym",
                        "numpy",
                        "pybullet"])
